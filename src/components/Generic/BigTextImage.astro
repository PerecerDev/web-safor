---
import {

} from "../../styleConstants.js";

const styleConstants = [

].join(' ');

const {title, text, image, side, color, bg} = Astro.props;

---
<section id="second-section" data-theme="orange" class=" w-full relative ">
    <aside class=`w-full relative z-20 p-12 pt-32 text-${side} text-${color} ${bg}`>
        
        <h2 class="w-full font-serif text-6xl font-bold mb-10 text-white">{title}</h2>
        <h3 class=`w-full font-sans text-4xl font-thin text-${side} text-stone-500 mb-20 pl-52`>{text}</h3>

    </aside>
    <aside class="w-full overflow-hidden absolute z-20 -bottom-48 ">
        <img src=`${image}` alt=`${title}` class=" w-80 h-auto block"/>
    </aside>
</section>