---

import {} from "../../styleConstants.js";
const styleConstants = [].join(' ');

const {title, side, color, bg} = Astro.props;

---

<section data-theme="white" class=`grid w-full items-center p-12 text-${side} text-${color} ${bg}`>

    <h1 class=" w-full font-serif leading-40px text-6xl font-bold">{title}</h1>

</section>