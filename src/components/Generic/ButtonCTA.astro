---

interface Props{
    text: string;
    link: string;
}

const { button, link } = Astro.props;

import {

} from "../../styleConstants.js";

const styleConstants = [

].join(' ');

---

<div class="flex w-full px-6 pb-20">
    <a href={link} class={"flex-1 w-full py-8 text-center bg-gradient-to-br from-blue-900 to-blue-950 text-white rounded-md text-3xl font-sans font-thin shadow-xl shadow-blue-800/40 "}>
        {button}
    </a>
</div>
